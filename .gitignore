# Build output
target/
tools/bin/

# Stale wasm-pack output — canonical output is /pkg (tracked, needed for Pages/deploy)
glimr/pkg/

# Source photos (jpg) — but keep the fixture the test suite needs
*.jpg
!tests/test_a.jpg

# Gallery archives — keep Demo.zip (served sample); ignore the rest
*.zip
!Demo.zip

# Generated test artifacts (regenerated by `cargo test`)
tests/sample_*.png
tests/residual_wm.png

# Local editor workspace
glimpse-o-matic.code-workspace
