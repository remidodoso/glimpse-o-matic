[package]
name = "glimr"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
zip = { version = "2", default-features = false, features = ["deflate"] }
